# Requests

Each non-example `*.json` file is one bounded request addressed to the stable
project-agent role. IDs must be unique and must match the filename.

Requests describe intent and authority; they do not prove that a source claim
is true. Keep private data and credentials out of this directory.

