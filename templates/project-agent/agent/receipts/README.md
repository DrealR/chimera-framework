# Receipts

A receipt records what happened; it grants no new authority. Name it with its
`receipt_id`. Use exact Git and test evidence, and classify any peer claim before
placing it in durable current state.

